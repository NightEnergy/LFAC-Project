int globalX;
float globalF;
string hello;
bool flag;

begin_progr
    // 1. Arithmetic & Types
    globalX <- 10 + 5 * 2; 
    Print(globalX); // Should print 20

    globalF <- 10.5 / 2.0;
    Print(globalF); // Should print 5.25

    hello <- "Hello World";
    Print(hello);

    // 2. Negative numbers & Unary minus
    int neg;
    neg <- -5;
    Print(neg); // Should print -5
    Print(10 + -2); // Should print 8

    // 3. Boolean Logic (AND/OR) & Comparison
    flag <- true;
    bool check;
    check <- (globalX > 10) && (neg < 0); 
    Print(check); // Should print true (1)

    check <- (globalX == 5) || flag;
    Print(check); // Should print true (1)

    // 4. While Loop
    int i;
    i <- 0;
    Print("Starting Loop:");
    while(i < 3) {
        Print(i);
        i <- i + 1;
    }

    // 5. If/Else
    if(globalX >= 20) {
        Print("X is big");
    } else {
        Print("X is small");
    }

end_progr
